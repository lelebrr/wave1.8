#pragma once

#include <stdint.h>

// Modelo placeholder extremamente pequeno apenas para permitir compilação.
// Substitua por um array gerado a partir de neura9_defense_model.tflite
// (via xxd ou script de conversão) quando tiver o modelo real treinado.
extern const unsigned char neura9_defense_model_tflite[];
extern const unsigned int neura9_defense_model_tflite_len;